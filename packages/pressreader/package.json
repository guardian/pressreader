{
  "name": "pressreader",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@types/node": "18.13.0",
    "aws-sdk": "^2.1362.0"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn src/handler.ts",
    "typecheck": "tsc -noEmit",
    "format": "prettier --write \"src/**/*.ts\"",
    "build": "tsc --noEmit & esbuild src/handler.ts --bundle --minify --outfile=dist/handler.js --external:aws-sdk --platform=node",
    "test": "jest --detectOpenHandles --config ../../jest.config.js --selectProjects pressreader"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guardian/newsroom-resilience-platform.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/guardian/newsroom-resilience-platform/issues"
  },
  "homepage": "https://github.com/guardian/newsroom-resilience-platform#readme",
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
